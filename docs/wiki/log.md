# Wiki Log

- 2026-08-26 — Initial build. Moved 10 raw spec files (design-overview, homepage, solutions, disclosures, investor-services, schedule, calculator, seo-tools, whatsapp-contact, privacy-policy_terms) plus empty file-structure.md from `docs/Abhijit-Sinha-Website/` into `docs/sources/`. Created wiki pages: design-system, regulatory-compliance, site-architecture, page-homepage, page-solutions, page-disclosures, page-investor-services, page-schedule, calculators, seo-and-metadata, contact-channels, legal-copy, index. Explicitly excluded `Images/` and `Important Docs/` from the wiki scope per user instruction (Important Docs is sensitive).
- 2026-08-26 — Scaffolded the Astro+Tailwind site and added deployment-domain.md documenting the deferred custom-domain cutover (GoDaddy/Cloudflare) decision.
- 2026-08-26 — Post-launch fixes from live review: hero rebuilt to show the (already fully-designed) banner artwork clean and uncropped instead of overlaying duplicate text on it; shrinking sticky header; confirmed ARN validity dates against Important Docs/ARN Registration.pdf and removed the "pending confirmation" caveat; profile photo reframed to stop cropping the face; added a visible active-nav-page indicator. Updated design-system.md, site-architecture.md, page-homepage.md, and regulatory-compliance.md to match.
- 2026-08-28 — Admin dashboard Phases 0–1. Stood up Supabase project `cebfypcoyqiegwuahmun`: `admin_allowlist` (two accounts) gating every RLS policy via `is_admin()`, plus `leads`, `site_content`, `videos`, `posts`, and an append-only `content_audit` fed by triggers. Deployed the `submit-lead` edge function and pointed `PUBLIC_FORM_ENDPOINT` at it, which activates the two contact forms that had shipped disabled since launch. **Removed the folio-number field** from `ContactForm.astro` — storing financial identifiers via a web form is a DPDP liability with no operational gain; folios are now collected over WhatsApp/phone instead. Added Privacy Policy §5.1 disclosing what enquiry data is stored, where, and for how long (24 months), and fixed the hardcoded `compliance@` mailto that bypassed `site.ts` and pointed at an unprovisioned mailbox. Hardened all `SECURITY DEFINER` functions after the Supabase linter flagged them (6 warnings → 1 accepted). Evaluated Decap CMS and Sanity/Strapi and rejected both — neither handles form submissions. New wiki pages: admin-dashboard.md, data-model.md. Phases 2–5 (dashboard UI, editable content, videos/notes, audit snapshot) are planned but not built.
- 2026-08-26 — Second round of homepage fixes: header now overlays the hero (translucent, out-of-flow) instead of pushing it down, since the 2:1 banner rendered taller than the viewport and hid every call-to-action; the CTA band is `sticky bottom-0` so it's visible on first load and releases naturally at the hero's end. Nav highlight now also follows `#about` via an IntersectionObserver scroll-spy (styling moved onto `aria-[current=page]:` variants so both the server-rendered and client-side paths share one mechanism). WhatsApp button collapsed to icon-only with a hover/focus-reveal label. Added a "Connect on LinkedIn" button under the About profile. Updated design-system.md, site-architecture.md, page-homepage.md, and contact-channels.md.
- 2026-09-02 — Mobile horizontal-scroll bug, found and killed after five deploys. A reporting phone was panning every page sideways; the on-device probe returned **clientWidth 411 against innerWidth 876** with 465px of scroll, exactly the difference. Four separate causes, all real: carousel arrow buttons revealed below `sm` with no display class (a 6px overflow); the floating WhatsApp button and privacy notice anchored with `right-6`/`inset-x-0`, which a fixed box resolves against the *window* rather than the layout viewport, parking the button at x=852; a `sr-only` span (position:absolute, no positioned ancestor) escaping the `overflow-x-auto` wrapper on the new RTA table links; and finally the carousel tracks themselves, whose removal collapsed the window/viewport mismatch outright. `overflow-x: clip` on the root was tried and reverted — it does not clip fixed-position boxes and would only have masked the next bug. **Knowledge Corner carousels are now a plain 3-up grid** and `Carousel.astro` plus its script are deleted. Also fixed in the same pass: "Show more" appearing on every card (Tailwind's preflight `[hidden]` rule loses to the `inline-flex` utility, so the button was never hidden and the overflow measurement decided nothing). New wiki page: mobile-viewport-pitfalls.md — read it before adding anything that floats, clips or scrolls sideways.
