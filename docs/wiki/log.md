# Wiki Log

- 2026-08-26 — Initial build. Moved 10 raw spec files (design-overview, homepage, solutions, disclosures, investor-services, schedule, calculator, seo-tools, whatsapp-contact, privacy-policy_terms) plus empty file-structure.md from `docs/Abhijit-Sinha-Website/` into `docs/sources/`. Created wiki pages: design-system, regulatory-compliance, site-architecture, page-homepage, page-solutions, page-disclosures, page-investor-services, page-schedule, calculators, seo-and-metadata, contact-channels, legal-copy, index. Explicitly excluded `Images/` and `Important Docs/` from the wiki scope per user instruction (Important Docs is sensitive).
- 2026-08-26 — Scaffolded the Astro+Tailwind site and added deployment-domain.md documenting the deferred custom-domain cutover (GoDaddy/Cloudflare) decision.
- 2026-08-26 — Post-launch fixes from live review: hero rebuilt to show the (already fully-designed) banner artwork clean and uncropped instead of overlaying duplicate text on it; shrinking sticky header; confirmed ARN validity dates against Important Docs/ARN Registration.pdf and removed the "pending confirmation" caveat; profile photo reframed to stop cropping the face; added a visible active-nav-page indicator. Updated design-system.md, site-architecture.md, page-homepage.md, and regulatory-compliance.md to match.
- 2026-08-28 — Admin dashboard Phases 0–1. Stood up Supabase project `cebfypcoyqiegwuahmun`: `admin_allowlist` (two accounts) gating every RLS policy via `is_admin()`, plus `leads`, `site_content`, `videos`, `posts`, and an append-only `content_audit` fed by triggers. Deployed the `submit-lead` edge function and pointed `PUBLIC_FORM_ENDPOINT` at it, which activates the two contact forms that had shipped disabled since launch. **Removed the folio-number field** from `ContactForm.astro` — storing financial identifiers via a web form is a DPDP liability with no operational gain; folios are now collected over WhatsApp/phone instead. Added Privacy Policy §5.1 disclosing what enquiry data is stored, where, and for how long (24 months), and fixed the hardcoded `compliance@` mailto that bypassed `site.ts` and pointed at an unprovisioned mailbox. Hardened all `SECURITY DEFINER` functions after the Supabase linter flagged them (6 warnings → 1 accepted). Evaluated Decap CMS and Sanity/Strapi and rejected both — neither handles form submissions. New wiki pages: admin-dashboard.md, data-model.md. Phases 2–5 (dashboard UI, editable content, videos/notes, audit snapshot) are planned but not built.
- 2026-08-26 — Second round of homepage fixes: header now overlays the hero (translucent, out-of-flow) instead of pushing it down, since the 2:1 banner rendered taller than the viewport and hid every call-to-action; the CTA band is `sticky bottom-0` so it's visible on first load and releases naturally at the hero's end. Nav highlight now also follows `#about` via an IntersectionObserver scroll-spy (styling moved onto `aria-[current=page]:` variants so both the server-rendered and client-side paths share one mechanism). WhatsApp button collapsed to icon-only with a hover/focus-reveal label. Added a "Connect on LinkedIn" button under the About profile. Updated design-system.md, site-architecture.md, page-homepage.md, and contact-channels.md.
- 2026-09-02 — Mobile horizontal-scroll bug, found and killed after five deploys. A reporting phone was panning every page sideways; the on-device probe returned **clientWidth 411 against innerWidth 876** with 465px of scroll, exactly the difference. Four separate causes, all real: carousel arrow buttons revealed below `sm` with no display class (a 6px overflow); the floating WhatsApp button and privacy notice anchored with `right-6`/`inset-x-0`, which a fixed box resolves against the *window* rather than the layout viewport, parking the button at x=852; a `sr-only` span (position:absolute, no positioned ancestor) escaping the `overflow-x-auto` wrapper on the new RTA table links; and finally the carousel tracks themselves, whose removal collapsed the window/viewport mismatch outright. `overflow-x: clip` on the root was tried and reverted — it does not clip fixed-position boxes and would only have masked the next bug. **Knowledge Corner carousels are now a plain 3-up grid** and `Carousel.astro` plus its script are deleted. Also fixed in the same pass: "Show more" appearing on every card (Tailwind's preflight `[hidden]` rule loses to the `inline-flex` utility, so the button was never hidden and the overflow measurement decided nothing). New wiki page: mobile-viewport-pitfalls.md — read it before adding anything that floats, clips or scrolls sideways.
- 2026-09-03 — **Custom domain cut over: the site is live and indexable at `https://abhijitsinha.in`.** GoDaddy DNS now carries `A @ → 216.198.79.1` and `CNAME www → 50fdc8d963fd4e2e.vercel-dns-017.com` (a per-account host, not the documented `cname.vercel-dns.com`), with Vercel 308-ing `www` to the apex; `SITE_URL` was repointed and `public/robots.txt`'s blanket `Disallow: /` lifted, keeping `Disallow: /admin`. Four things the runbook did not predict, all presenting as *correct records that do nothing*: Vercel now issues `216.198.79.1` rather than the documented `76.76.21.21`; the apex carried **three** A records, two of them GoDaddy forwarding endpoints whose delete/edit controls were greyed out because **the forwarding feature owns them** — they can only be removed by deleting the forwarding entry, and until then DNS round-robins two visitors in three onto the parking page; that forwarding pointed apex → `www` (301) against Vercel's `www` → apex (308), a redirect loop waiting to happen; and Vercel's "Vercel DNS" nameserver tab is a **trap**, since taking it moves the whole zone including the MX records for `support@abhijitsinha.in`. Verified live: 8 routes 200 + 404, canonical/`og:url` on the apex, sitemap resolving with zero `/admin` URLs, article body present in view-source. **Left open:** `abhijit-sinha-website.vercel.app` still returns 200 and must be redirected — an indexable duplicate of a regulated-content site — and Supabase Auth's Site URL / redirect allowlist still needs `https://abhijitsinha.in/admin` or admin sign-in fails silently. Updated deployment-domain.md, seo-and-metadata.md, index.md.
- 2026-09-03 — Fixed `[object Object], [object Object], [object Object], [object Object]` rendering in the Annual Trail Commission Schedule intro on `/disclosures`. `empanelledAmcs` was widened from `string[]` to `AmcPartner[]` when the footer gained AMC logo tiles; the footer was updated to the new shape and the disclosures page's `.join(', ')` was not, so it stringified the objects. Caught nothing at build time — valid JS, clean `astro check`, green deploy, defect visible only on the rendered statutory page. Updated page-disclosures.md, which also had the table's columns recorded as "Year-1 / Year-2+" when the shipped table is "Years 1–3 / Year 4 Onwards".
- 2026-09-03 — Closed both cutover follow-ups. `abhijit-sinha-website.vercel.app` now 308s to the apex **preserving the path**, ending the ~1h window where the full regulated-content site was crawlable on two hostnames; Supabase Auth's Site URL and the `/admin` redirect allowlist were repointed at the custom domain (set, but not yet proven by a real magic-link sign-in — a wrong entry there fails silently with no error surfaced anywhere). Google Search Console reports "Couldn't fetch" on `https://abhijitsinha.in/sitemap-index.xml`; the sitemap itself verifies clean as Googlebot (200, `application/xml`, no redirect, allowed by robots), so this is Search Console's pre-first-crawl default state and/or Google holding stale DNS from the 1-hour TTL on the old parking records, not a site fault.
- 2026-09-03 — Cutover signed off. Admin magic-link sign-in and both contact forms confirmed working on `abhijitsinha.in`, which closes the last two things the runbook could not prove by `curl` — the Auth redirect allowlist and the `submit-lead` → `leads` → Web3Forms chain both fail silently when wrong, so a manual pass was the only real check. Added a **"What is still open"** table to deployment-domain.md covering the four loose ends (Search Console's pre-crawl "Couldn't fetch", the unverified WhatsApp OG preview, the unverified `VERCEL_DEPLOY_HOOK_URL` secret behind the admin Publish button, and the now-unblocked Resend migration) plus one unrelated deadline that nothing else tracks: `commissionPeriod` in `site.ts` is a dated statutory claim on `/disclosures` that expires **30 September 2026** and must be refreshed from the AMC rate cards.
- 2026-09-03 — **Published the full 14-AMC empanelment and split it away from the commission claim.** The empanelment list grew from 4 to 14, but `empanelledAmcs` was doing double duty — footer logo wall *and* the AMCs the `/disclosures` commission ranges are stated to derive from. Adding ten names would have silently widened a dated statutory claim to cover eight AMCs whose rate cards nobody has seen, so `AmcPartner` gained `rateCardOnFile` and `site.ts` now derives `rateCardAmcs`; only that list may be named beside the table, and the page says outright that the rest are not reflected in the ranges. Axis and quant cards arrived mid-change, taking the covered set to six and moving four bounds: Hybrid ceiling 1.15%→1.16% (quant Equity Savings at its highest AUM slab, perpetual trail so both columns), Solution-Oriented floor 0.55%→0.51% (Axis Children's), Debt floor 0.12%→0.08% (Axis Banking & PSU), Liquid floor 0.03%→0.02% (Axis Overnight). quant's SIF long-short card is deliberately excluded — different SEBI product category — and the quant card on file lapsed 31 August, which is now tracked in deployment-domain.md. The logo wall moved from the footer to a section on `/disclosures` directly under the table it qualifies; the footer keeps a one-line pointer plus the non-endorsement sentence. Ten logos normalised via sharp (trim to the mark's bounds, cap at 480px — the Edelweiss SVG rasterised to 11134px wide) and a `max-w-[min(100%,9rem)]` cap added, because the per-logo `maxH` values were tuned against the footer's 144px tile and the wider grid tiles let marks past 4:1 balloon to ~6500px² against ~4700px². Verified: `astro check` and build clean, 14 logos 200 with no `[object Object]`, every mark inside its tile, areas 3498–4943px², and **zero horizontal overflow at 375px** on `/disclosures` and the homepage. Updated page-disclosures.md, design-system.md (new "AMC logo tiles" section), regulatory-compliance.md, deployment-domain.md, CLAUDE.md.
- 2026-09-03 — Footer decluttered, bot gate added in front of the lead form, and the public repo hardened. **Footer:** the link row was `[...nav, primaryCta, Privacy Policy, Terms of Use]` plus all three redressal links plus Privacy Choices — thirteen full-size items in one flat row. It is now a short inline `text-xs` row of nine middot-separated items (`footerLinks` in `site.ts`), placed *below* the copyright line; `About`/`Schedule a Meeting` dropped as header duplicates, CAMS/KFintech dropped to `/investor-services`, SEBI SCORES kept because the grievance channel is a statutory footer expectation. Separators are real `aria-hidden` spans, not `after:content-['·']` — Tailwind's scanner generated no rule at all for the escaped-character arbitrary value and the separators silently vanished. **Bot protection:** new `verify-lead` edge function sits in front of `submit-lead` and forwards to it, so `submit-lead` is never redeployed and repointing `PUBLIC_FORM_ENDPOINT` remains a one-line rollback. Checks: origin allowlist → 16KB cap → honeypot → 2500ms dwell → field caps → per-IP rate limit (5/15 min, hashed, never stored) → Cloudflare Turnstile. The gap it closes is real: `submit-lead`'s only limit is 3/hour *per email*, so a bot varying the address was throttled by nothing. Turnstile is **fail-open while `TURNSTILE_SECRET_KEY` is unset, fail-closed once set** — an unconfigured secret must not take the only lead channel offline. Client side adds the widget, the dwell field and `maxlength` caps, keeps the button disabled after success, and calls `turnstile.reset()` after a failure (tokens are single-use; without the reset every retry is rejected and the form looks broken). Disclosed in Privacy Policy §5.6, and the privacy notice's "no cookies" claim was narrowed to "no tracking cookies" with the Turnstile exception named — the old blanket claim would have become a false disclosure. **Security:** first `vercel.json` — enforcing CSP plus HSTS, `nosniff`, `Referrer-Policy`, `X-Frame-Options`, `Permissions-Policy`, COOP. The audit found **no committed credential and none ever committed**, but `.env` and `.env.example` were byte-identical, so the tracked "template" was the live config file; `.env.example` is now placeholders only. `vite.build.assetsInlineLimit: 0` had to be added to `astro.config.mjs` and is load-bearing: Astro was inlining four sub-4KB scripts per page straight into the HTML, and `script-src 'self'` cannot survive that without `'unsafe-inline'` (defeats the point) or sha256 hashes (go stale silently on the next script edit). CSP was verified by serving `dist/` from a throwaway Node server sending the same headers as report-only and walking every route — Vercel headers do not apply to `astro dev` or `astro preview`, so a CSP cannot be tested by running the dev server. New wiki page: security-hardening.md. **Left open:** the Cloudflare Turnstile widget must be created (free account, no DNS move), `TURNSTILE_SECRET_KEY` set in Supabase, `verify-lead` deployed with `--no-verify-jwt`, and Vercel's `PUBLIC_TURNSTILE_SITE_KEY` + repointed `PUBLIC_FORM_ENDPOINT` set — until then the site keeps posting straight at `submit-lead` exactly as before. GitHub secret scanning + push protection still to be enabled.
- 2026-09-03 — `verify-lead` deployed to Supabase (v1, ACTIVE, `verify_jwt: false`) and verified live with nine curl probes: unallowlisted origin 403, missing `Origin` 403, `GET` 405, preflight 204 reflecting only the allowlisted origin, `localhost:4321` accepted, over-long `name` 400 with an actionable message, filled honeypot 200 with the normal success wording, sub-threshold dwell 200 likewise — and **neither of the last two wrote a row to `leads`**, confirming the silent-success short-circuits forward nothing. A valid-looking submit with no Turnstile token returned 403, which is the positive proof that `TURNSTILE_SECRET_KEY` is set and the gate is fail-CLOSED (the fail-open branch would have forwarded). Edge logs match the nine probes exactly. Also confirmed while in the database: `leads` row id 5 (the Phase-1 test record holding a real email and mobile) has been deleted — that long-standing wiki item is closed. GitHub Secret Protection and Push protection are both enabled; note GitHub renamed "secret scanning" to "Secret Protection", so the old label finds nothing in settings. **Proven end to end the same day.** Shipped in 8d0ab7f; live headers verified (all seven present, CSP enforcing), the built page carries `data-endpoint` pointing at `verify-lead` and the Turnstile site key, `window.turnstile` loads with zero CSP violations, and a real browser submission landed as `leads` row 11 with every field intact and the three gate fields (`cf-turnstile-response`, `form_dwell_ms`, `website`) correctly stripped before the forward. One note for whoever tests this next: Turnstile issued no token to an automated browser — the widget mounted its container and stopped — so the token step cannot be verified by automation, only by a person in a normal browser. That is the control working, not a fault. A Vercel gotcha also surfaced: `PUBLIC_*` env vars must be typed **Config**, not **Secret**; Secret-typed values are write-only and cannot be converted afterwards, so the variable has to be deleted and re-created.
- 2026-09-03 — Ingest pass over the footer/bot-gate/hardening change. Most of it was already written up as it shipped; the real gap was `legal-copy.md`, which still described only the **source spec** and had never tracked the shipped Privacy Policy — no §5.1 (Aug 28), no §5.2–5.5 (Sep 1), and now no §5.6. Added a "What actually shipped" table of the live §1–6 structure with the date each subsection was added, plus the standing rule it encodes: adding or widening any collection, or adding any third party, requires a Privacy Policy edit **in the same change**. Also recorded there that §5.6 came with a second, easy-to-miss edit — `PrivacyNotice.astro` and §5.2 both claimed "no cookies", a blanket zero that Turnstile's challenge cookie would have made false, so both were narrowed to "no *tracking* cookies" with the exception named; a banner that misstates the site is exactly what that component exists to avoid. Two stale claims corrected in place in `contact-channels.md`: the channel table still listed the **Folio (optional)** field that the same page's own paragraph says was deliberately removed (a page contradicting itself), and the scheduler row still read "tool TBD" when `scheduler.provider` has been `calendly` since launch. Cross-linked legal-copy ↔ contact-channels ↔ security-hardening ↔ data-model in both directions, and cleared `.pending-ingest` (which had also been carrying four already-ingested paths from 61fb249 — the hook appends, nothing truncates it, so the queue accumulates until an ingest runs).
